name: Ping Site

on:
  schedule:
    - cron: "*/15 * * * *" # Runs every 15 minutes
  workflow_dispatch: # Manual trigger option

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl site
        run: curl https://sandbox-sample-d8sy56hhw-useyour-points.vercel.app
